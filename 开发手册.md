# DeskBud 网站开发手册

> 站点：DeskBud.xyz — 桌面宠物作品展示分享站
> 仓库：gooddaysboy/deskbud（**仅放网站**，桌面宠物代码在 gooddaysboy/pet，两仓严格隔离）
> 部署：腾讯云 EdgeOne Pages，连接 deskbud 仓库 main 分支，根目录 / 部署，无需构建
> 最后更新：2026-08-24

## 一、项目定位
面向桌面宠物爱好者的作品展示分享静态站点。简洁护眼、PC 优先兼容移动端。
Slogan：陪伴你的桌面宠物伙伴。

## 二、目录结构
```
website/
  index.html        首页（标题/slogan/分类导航/热门预览）
  list.html         作品列表（分类筛选 + 排序：最新/下载量/热门）
  detail.html       作品详情（?id= 驱动；图文/GIF/视频 + 下载 + 统计）
  assets/css/base.css   全局样式（柔和低饱和护眼配色，CSS 变量统一）
  assets/js/site.js     公共逻辑：加载 works.json、渲染、排序、不蒜子统计
  data/works.json       作品数据源（唯一数据入口）
  works/<id>/           各作品素材（图片/GIF/视频/下载包）
  README.md             使用与部署说明
```

## 三、技术决策（2026-08-24 定稿）
- **纯静态 HTML/CSS/JS，零构建**：EdgeOne 直接部署根目录，最稳最简单。
- **数据驱动**：所有作品信息集中在 `data/works.json`，页面 JS 读取渲染，加作品只改 JSON。
- **统计 = 不蒜子 busuanzi**：全站 + 单页访问量（脚本 `busuanzi.pure.mini.js` 注入）。
  ⚠️ 下载次数**非真实下载计数**——当前用作品 `downloads` 字段静态展示（近似），纯静态站无法精确统计下载。后续若要真实下载数，需接计数 API/边缘函数（待定）。
- **配色**：低饱和护眼。品牌色 `--brand` 鼠尾草绿 #7c9a8e，强调色 `--accent` 暖陶土 #c8a98a。

## 四、本地预览（务必先预览再推）
工作区是 Git 仓库，直接双击 html 会因 `file://` 下 fetch 失败（浏览器禁止 file 协议读本地 JSON，热门/分类全空白，只剩静态 Hero 区）。**必须**用本地 HTTP 服务器：

**方式一：Python（推荐，几乎所有机器都有）**
```bash
cd D:\deskbud\website
python -m http.server 8080
# 浏览器打开 http://localhost:8080  （注意是 http:// + localhost:8080，不是 D:/deskbud/website/index.html）
```

**方式二：VS Code Live Server 插件**
装 `Live Server` 插件 → 右键 `index.html` → Open with Live Server。

⚠️ 老曹 2026-08-24 踩坑：第一次本地预览双击了 `D:/deskbud/website/index.html`（file://），导致所有 JS 渲染区空白、以为是代码问题。**永远用 http://localhost**。

校验点：首页热门/分类渲染、列表筛选排序、详情页媒体与下载按钮、不蒜子计数出现。

## 五、提交与推送（关键经验，照搬宠物项目）
- **🔴 铁律：本地真实浏览器渲染通过才推送（老曹 2026-08-24 怒斥后立规）**。curl 200 / node 模拟数据不算验证。JS 渲染页面的运行时错误只能靠真实浏览器暴露。**提交前必须跑一遍**：
  ```bash
  cd D:\deskbud\website
  python -m http.server 8080   # 起本地服务
  # 方式A（必做）：WorkBuddy 自带浏览器预览确认（老曹 2026-08-24 明确：每次修改都要用）
  #   present_files 打开 http://localhost:8080/index.html 人工看效果
  # 方式B（辅助）：系统 Chrome headless 数卡片，防人工漏看
  "/c/Program Files/Google/Chrome/Application/chrome.exe" --headless --disable-gpu --no-sandbox --virtual-time-budget=6000 --dump-dom "http://localhost:8080/index.html" 2>/dev/null | grep -o 'class="card"' | wc -l
  # 期望：index 9 张卡片（4 热门+5 分类预览）；list 6 张；detail 含标题/下载按钮
  ```
  渲染结果对不上预期，**不许提交**。改 JS/CSS 后**必须升版本号**（`?v=2` → `?v=3`…）防客户端缓存旧代码。
- 本目录 `D:\deskbud\website\` 是**独立 Git 仓库**（remote=gooddaysboy/DeskBud），不是 pet 仓库子模块。
- 宠物仓库 `D:\deskbud\.gitignore` 已加 `website/` 忽略，避免嵌套 git 冲突。
- **⚠️ 仓库名大小写**：GitHub 上仓库已重命名为 `DeskBud`（大写 B），原 `deskbud` 推送会提示 "repository moved"。remote 必须用 `git@github.com:gooddaysboy/DeskBud.git`（scp 风格，见下）。
- **家里机（D:\deskbud）推送铁律**：`~/.gitconfig` 把 `git@github.com:` 改写 https，沙箱内 HTTPS 因无交互凭证会失败。
  必须用 `ssh://` 形式 + 指定 key：
  ```bash
  cd D:\deskbud\website
  GIT_SSH_COMMAND="ssh -o BatchMode=yes -o StrictHostKeyChecking=no -i ~/.ssh/id_ed25519" \
    git push ssh://git@github.com/gooddaysboy/DeskBud.git main
  ```
  （unit 机规则相反，用 `git@github.com:` scp 风格；两台机正好相反，别记混）
- **首次推送（2026-08-24）**：远程 main 误放了 flutter 宠物历史，按老曹确认（deskbud=网站，pet=宠物）强推覆盖：
  ```bash
  ... git push --force ssh://git@github.com/gooddaysboy/DeskBud.git main
  ```
  ⚠️ `--force` 破坏性，仅确认网站专用仓、宠物已在 pet 仓备份时用。
- 推送后 EdgeOne Pages 自动构建发布（无需仓库内 workflow）。
- **部署配置（EdgeOne Pages）**：连 GitHub 仓库 `gooddaysboy/DeskBud`，分支 `main`，构建目录 `/`（根），无构建命令（纯静态），输出即根。

## 六、协作铁律（老曹 2026-08-24 明确）
- **关键步骤/修改经验必写记忆 + 开发手册**：每次代码或配置变更，同步更新本手册（改了什么、为什么、参数、坑），再提交/推送。
- **先本地预览确认无误再推**。
- **仓库分工**：deskbud=网站，pet=宠物，绝不混推。

## 七、已知坑 / 待办
- **⚠️ JS this 绑定坑（2026-08-24 修复）**：`site.js` 的 `cardHTML` 内部曾用 `this.thumbHTML()` 等，但页面调用 `list.map(SITE.cardHTML)` 把方法当独立回调传入，`this` 丢失 → 报 `this.thumbHTML is not a function` → 所有卡片渲染中断（页面只剩 Hero 静态区，看似"代码没内容"）。**修复：`cardHTML` 内部一律用 `SITE.xxx()` 显式调用，不依赖 `this`。** 新增方法同理。
- **JS 渲染页面必须用真实浏览器验证**：node 模拟只能验数据/排序逻辑，验不出 `this` 绑定等运行时错误。改 JS 后务必 Chrome headless 渲染一遍（`chrome --headless --dump-dom http://localhost:8080/index.html` 看卡片数）。
- 下载次数统计为静态近似，需真实计数时再接第三方。
- 已填充真实素材：panda-orbit / panda-android（图/GIF/视频来自工作空间熊猫与 pet_video 素材）；其余 4 个示例作品用 SVG 占位封面，待替换真实作品。
- 不蒜子为第三方服务，若其域名失效需换方案（51.la / 百度统计）。

## 八、作品上架指南（老曹问答：怎么上传/管理作品？）
纯静态站**没有后台**，上传作品 = 2 件事：**放文件 + 改 JSON**。流程如下：

### 1. 放素材文件
- 每个作品一个文件夹：`works/<作品id>/`（id 是英文小写+连字符，如 `panda-orbit`）。
- 素材命名约定（site.js / detail.html 按文件名逻辑渲染）：
  - `cover.png` / `thumb.png`：列表卡片缩略图（thumb 优先）；`thumb` 缺省时用 `cover`。
  - `1.png`、`2.png`…：详情页正文图片（可多张）。
  - `walk.gif` 等 GIF：详情页动图。
  - `preview.mp4`：详情页演示视频（建议 ≤5MB，过大请压缩后再放）。
- 图片建议 1280 内、压缩过，GIF 用压缩版（参考 `gif_compress/`），保证加载流畅。

### 2. 在 `data/works.json` 注册作品
每个作品是 `works` 数组里的一个对象，必填字段：
```json
{
  "id": "你的作品id",            // 唯一，对应文件夹名
  "title": "作品标题",
  "category": "panda",           // 取 categories 里的 id：panda/cat/dog/fantasy/retro/tool
  "author": "作者名",
  "cover": "works/<id>/cover.png",
  "thumb": "works/<id>/thumb.png",
  "summary": "一句话简介（卡片上显示）",
  "description": "详细介绍（详情页显示，可用多行）",
  "media": [
    { "type": "image", "src": "works/<id>/1.png", "caption": "说明文字" },
    { "type": "gif",   "src": "works/<id>/walk.gif", "caption": "说明文字" },
    { "type": "video", "src": "works/<id>/preview.mp4", "caption": "说明文字" }
  ],
  "download": { "label": "按钮文案", "url": "真实下载链接" },
  "downloads": 100,              // 展示用下载数
  "date": "2026-08-24",
  "featured": false              // true=上首页热门
}
```
想下架一个作品：从 `works` 数组删掉它的对象即可。

### 3. 本地预览确认
```bash
cd D:\deskbud\website
python -m http.server 8080
# 打开 http://localhost:8080/list.html 看卡片、detail.html?id=<你的id> 看详情
```

### 4. 推送上线
```bash
cd D:\deskbud\website
git add -A && git commit -m "add: 作品 xxx"
GIT_SSH_COMMAND="ssh -o BatchMode=yes -o StrictHostKeyChecking=no -i ~/.ssh/id_ed25519" \
  git push ssh://git@github.com/gooddaysboy/DeskBud.git main
```
推完 EdgeOne Pages 自动重新发布，约 1-2 分钟生效。
